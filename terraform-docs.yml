formatter: "markdown"

recursive:
  enabled: true
  path: terraform

output:
  file: "README_TF.md"
  mode: replace

settings:
  color: false
  description: "true"
  hide-empty: true
  read-comments: true
  sensitive: true
  lockfile: true

sort:
  enabled: true
  by: required
